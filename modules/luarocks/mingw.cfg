-- etc/config.lua  global LuaRocks config file
--proxy = 'http://yourproxy.com
variables = {
    MSVCRT = 'm',  -- stop us linking against msvc80
    LIBFLAG = '-shared -Wl,-s ', -- strip by default
    LUALIB = 'liblua.dll.a',
}
